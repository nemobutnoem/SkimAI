diff --git a/FE/src/styles/global.css b/FE/src/styles/global.css
index 8082c71..4a97a38 100644
--- a/FE/src/styles/global.css
+++ b/FE/src/styles/global.css
@@ -270,42 +270,6 @@ a {
   gap: 12px;
 }
 
-.admin-section-nav {
-  display: flex;
-  flex-wrap: wrap;
-  gap: 8px;
-  padding: 8px;
-  border-radius: var(--radius-lg);
-  border: 1px solid var(--border-color);
-  background: var(--white);
-}
-
-.admin-section-link {
-  display: inline-flex;
-  align-items: center;
-  justify-content: center;
-  padding: 8px 14px;
-  border-radius: var(--radius-full);
-  border: 1px solid transparent;
-  color: var(--text-secondary);
-  font-size: 13px;
-  font-weight: 600;
-  background: var(--gray-50);
-  transition: all 0.15s ease;
-}
-
-.admin-section-link:hover {
-  color: var(--primary);
-  border-color: var(--primary-light);
-  background: var(--primary-bg);
-}
-
-.admin-section-link.active {
-  color: var(--white);
-  border-color: transparent;
-  background: var(--primary-gradient);
-}
-
 .hero {
   text-align: center;
   padding: 56px 28px;
@@ -1499,16 +1463,17 @@ code {
 .di-shell {
   max-width: 1000px;
   display: grid;
-  gap: 20px;
+  gap: 22px;
 }
 
 .di-page-top {
   display: flex;
-  align-items: center;
+  align-items: flex-start;
   justify-content: space-between;
+  gap: 16px;
 }
-.di-page-top h1 { font-size: 24px; }
-.di-page-top .hint { margin-top: 4px; }
+.di-page-top h1 { font-size: clamp(24px, 3vw, 32px); margin: 4px 0 6px; }
+.di-page-top .hint { margin-top: 0; }
 
 /* Data Input Card */
 .di-data-input-card {
@@ -1516,40 +1481,49 @@ code {
   border-radius: var(--radius-lg);
   padding: 24px;
   border: 1px solid var(--border-color);
+  box-shadow: var(--shadow-sm);
+}
+.di-card-header-row {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: 12px;
+  margin-bottom: 18px;
 }
-.di-data-input-card h4 { font-size: 16px; font-weight: 600; margin: 0 0 16px; }
+.di-data-input-card h4 { font-size: 16px; font-weight: 700; margin: 0; }
 .di-input-row { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-bottom: 14px; }
-.di-input-label { font-size: 13px; font-weight: 500; color: var(--text-secondary); margin-bottom: 6px; }
+.di-input-label { font-size: 12px; font-weight: 700; color: var(--text-secondary); margin-bottom: 8px; text-transform: uppercase; letter-spacing: 0.04em; }
 .di-input-field { width: 100%; }
 .di-keyword-box {
-  padding: 16px 18px;
+  padding: 18px 20px;
   border: 1px solid var(--border-color);
+  border-left: 4px solid var(--primary);
   border-radius: var(--radius-md);
-  background: linear-gradient(180deg, rgba(108, 92, 231, 0.08) 0%, rgba(108, 92, 231, 0.02) 100%);
-  margin-bottom: 16px;
+  background: linear-gradient(135deg, rgba(108, 92, 231, 0.06) 0%, rgba(108, 92, 231, 0.01) 100%);
+  margin-bottom: 18px;
 }
 .di-keyword-value {
-  font-size: 24px;
+  font-size: 26px;
   font-weight: 800;
-  color: var(--text-primary);
+  color: var(--primary-dark);
   line-height: 1.2;
   word-break: break-word;
-  margin-bottom: 8px;
+  margin-bottom: 6px;
 }
-.di-data-source-tabs { display: flex; gap: 8px; margin-top: 8px; }
+.di-data-source-tabs { display: flex; gap: 8px; margin-top: 8px; flex-wrap: wrap; }
 .di-data-source-tab {
-  padding: 6px 14px;
+  padding: 8px 16px;
   border-radius: var(--radius-full);
   font-size: 12px;
-  font-weight: 500;
+  font-weight: 600;
   border: 1px solid var(--border-color);
   background: var(--white);
   color: var(--text-secondary);
   cursor: pointer;
   font-family: var(--font-family);
-  transition: all 0.15s ease;
+  transition: all 0.2s ease;
 }
-.di-data-source-tab.active { background: var(--primary); color: var(--white); border-color: var(--primary); }
+.di-data-source-tab.active { background: var(--primary-gradient); color: var(--white); border-color: transparent; box-shadow: 0 2px 8px rgba(108, 92, 231, 0.3); }
 .di-data-source-tab:hover:not(.active) { border-color: var(--primary); color: var(--primary); }
 
 /* Section Cards */
@@ -1558,128 +1532,251 @@ code {
   border-radius: var(--radius-lg);
   padding: 24px;
   border: 1px solid var(--border-color);
+  box-shadow: var(--shadow-sm);
 }
 .di-section-title {
   display: flex;
   align-items: center;
   gap: 10px;
-  font-size: 18px;
+  font-size: 17px;
   font-weight: 700;
-  margin-bottom: 20px;
+  margin-bottom: 18px;
+  color: var(--text-primary);
 }
-.di-section-icon { font-size: 20px; }
 
 /* Key Finding */
 .di-key-finding {
-  background: var(--green-light);
+  background: linear-gradient(135deg, #e8fff8 0%, #f4fefb 100%);
   border-left: 4px solid var(--green);
-  padding: 14px;
-  border-radius: 0 var(--radius-md) var(--radius-md) 0;
+  padding: 16px 18px;
+  border-radius: 0 var(--radius-lg) var(--radius-lg) 0;
   margin-bottom: 20px;
 }
-.di-kf-label { font-size: 11px; font-weight: 700; color: var(--green); text-transform: uppercase; margin-bottom: 4px; }
-.di-key-finding p { font-size: 14px; color: var(--text-primary); line-height: 1.6; margin: 0; }
+.di-kf-label { font-size: 11px; font-weight: 700; color: var(--green); text-transform: uppercase; letter-spacing: 0.06em; margin-bottom: 6px; }
+.di-key-finding p { font-size: 14px; color: var(--text-primary); line-height: 1.65; margin: 0; }
 
 /* Stat Grid */
 .di-stat-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 14px; }
-.di-stat-box { text-align: center; padding: 20px; background: var(--primary-bg); border-radius: var(--radius-md); }
+.di-stat-box {
+  text-align: center;
+  padding: 20px 16px;
+  background: linear-gradient(135deg, #f0edff 0%, #faf9ff 100%);
+  border-radius: var(--radius-lg);
+  border: 1px solid rgba(108, 92, 231, 0.08);
+  transition: transform 0.2s ease;
+}
+.di-stat-box:hover { transform: translateY(-2px); }
 .di-stat-num { font-size: 28px; font-weight: 800; color: var(--primary); }
 .di-stat-num-text {
-  font-size: 20px;
-  line-height: 1.25;
+  font-size: 18px;
+  line-height: 1.3;
   word-break: break-word;
 }
-.di-stat-desc { font-size: 12px; color: var(--text-secondary); margin-top: 4px; }
+.di-stat-desc { font-size: 12px; color: var(--text-secondary); margin-top: 6px; font-weight: 600; }
+
+/* -- Trend Table -- */
+.di-trend-status {
+  font-size: 12px;
+  color: var(--text-secondary);
+  text-align: right;
+  margin-bottom: 12px;
+  font-weight: 500;
+}
 
-/* Media Signal Items */
-.di-signal-item { padding: 14px 0; border-bottom: 1px solid var(--border-color); }
-.di-signal-item:last-child { border-bottom: none; }
-.di-signal-item h5 { font-size: 15px; font-weight: 600; margin: 0 0 4px; }
-.di-signal-item p { font-size: 14px; color: var(--text-secondary); line-height: 1.5; margin: 0; }
+.di-trend-table {
+  border: 1px solid var(--border-color);
+  border-radius: var(--radius-lg);
+  overflow: hidden;
+}
 
-/* Trend panel */
-.di-trend-panel {
+.di-trend-table-head {
   display: grid;
-  gap: 14px;
-  padding: 12px 4px 4px;
+  grid-template-columns: 40px 1.2fr 1.2fr 1.5fr;
+  gap: 0;
+  padding: 10px 16px;
+  background: var(--gray-50);
+  border-bottom: 1px solid var(--border-color);
+}
+
+.di-tth {
+  font-size: 11px;
+  font-weight: 700;
+  text-transform: uppercase;
+  letter-spacing: 0.06em;
+  color: var(--text-secondary);
 }
-.di-trend-row {
+.di-tth-rank { text-align: center; }
+
+.di-trend-table-row {
   display: grid;
-  gap: 8px;
+  grid-template-columns: 40px 1.2fr 1.2fr 1.5fr;
+  gap: 0;
+  padding: 14px 16px;
+  border-bottom: 1px solid var(--border-color);
+  align-items: center;
+  transition: background 0.15s ease;
 }
-.di-trend-head {
-  display: flex;
-  justify-content: space-between;
-  gap: 12px;
-  align-items: baseline;
+.di-trend-table-row:last-child { border-bottom: none; }
+.di-trend-table-row:hover { background: var(--gray-50); }
+
+.di-trend-rank {
+  width: 24px;
+  height: 24px;
+  border-radius: 8px;
+  background: var(--primary-bg);
+  color: var(--primary-dark);
+  font-size: 12px;
+  font-weight: 700;
+  display: inline-flex;
+  align-items: center;
+  justify-content: center;
+  margin: 0 auto;
 }
-.di-trend-label {
+
+.di-trend-kw {
   font-size: 14px;
-  font-weight: 700;
+  font-weight: 600;
   color: var(--text-primary);
 }
+
 .di-trend-note {
   font-size: 12px;
   color: var(--text-secondary);
 }
-.di-trend-track {
-  height: 12px;
+
+.di-trend-bar-cell {
+  display: flex;
+  align-items: center;
+  gap: 10px;
+}
+
+.di-trend-bar-track {
+  flex: 1;
+  height: 8px;
   border-radius: var(--radius-full);
   background: var(--gray-100);
   overflow: hidden;
 }
-.di-trend-fill {
+
+.di-trend-bar-fill {
   height: 100%;
   border-radius: var(--radius-full);
   background: var(--primary-gradient);
+  transition: width 0.5s ease;
+}
+
+.di-trend-pct {
+  font-size: 12px;
+  font-weight: 700;
+  color: var(--primary-dark);
+  min-width: 36px;
+  text-align: right;
+}
+
+/* -- Signal Cards -- */
+.di-signal-grid {
+  display: grid;
+  gap: 12px;
+}
+.di-signal-card {
+  display: flex;
+  gap: 14px;
+  padding: 16px 18px;
+  border-radius: var(--radius-lg);
+  border: 1px solid var(--border-color);
+  border-left: 4px solid var(--primary);
+  background: var(--white);
+  transition: transform 0.2s ease, box-shadow 0.2s ease;
+}
+.di-signal-card:nth-child(1) { border-left-color: var(--blue); background: linear-gradient(135deg, #f5faff 0%, #fff 100%); }
+.di-signal-card:nth-child(2) { border-left-color: var(--green); background: linear-gradient(135deg, #f4fefb 0%, #fff 100%); }
+.di-signal-card:nth-child(3) { border-left-color: #e17055; background: linear-gradient(135deg, #fffdf5 0%, #fff 100%); }
+.di-signal-card:hover {
+  transform: translateY(-2px);
+  box-shadow: 0 6px 20px rgba(0, 0, 0, 0.05);
+}
+.di-signal-icon {
+  width: 38px;
+  height: 38px;
+  border-radius: 12px;
+  background: rgba(255, 255, 255, 0.8);
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  font-size: 18px;
+  flex-shrink: 0;
+  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.06);
 }
+.di-signal-card h5 { font-size: 14px; font-weight: 700; margin: 0 0 4px; color: var(--text-primary); }
+.di-signal-card p { font-size: 13px; color: var(--text-secondary); line-height: 1.55; margin: 0; }
 
 /* Sentiment */
 .di-sentiment-two-col { display: grid; grid-template-columns: 1fr 1fr; gap: 28px; }
-.di-sentiment-bars { display: grid; gap: 10px; }
+.di-sentiment-bars { display: grid; gap: 12px; }
 .di-sentiment-bar-row { display: flex; align-items: center; gap: 14px; }
-.di-sentiment-bar-label { min-width: 60px; font-size: 14px; font-weight: 500; }
-.di-sentiment-bar-track { flex: 1; height: 20px; background: var(--gray-100); border-radius: var(--radius-full); overflow: hidden; }
+.di-sentiment-bar-label { min-width: 68px; font-size: 13px; font-weight: 600; }
+.di-sentiment-bar-track { flex: 1; height: 22px; background: var(--gray-100); border-radius: var(--radius-full); overflow: hidden; }
 .di-sentiment-bar-fill { height: 100%; border-radius: var(--radius-full); transition: width 0.6s ease; }
-.di-sentiment-bar-value { min-width: 40px; font-size: 14px; font-weight: 600; text-align: right; }
-.di-discussion-topic { display: flex; justify-content: space-between; padding: 8px 0; font-size: 14px; }
+.di-sentiment-bar-value { min-width: 42px; font-size: 14px; font-weight: 700; text-align: right; }
+.di-topics-title { font-size: 15px; font-weight: 700; margin: 0 0 10px; }
+.di-topics-list { display: grid; gap: 2px; }
+.di-discussion-topic {
+  display: flex;
+  justify-content: space-between;
+  padding: 10px 12px;
+  font-size: 14px;
+  border-radius: var(--radius-md);
+  transition: background 0.15s ease;
+}
+.di-discussion-topic:hover { background: var(--gray-50); }
 .di-topic-name { color: var(--text-secondary); }
-.di-topic-change { font-weight: 600; color: var(--green); }
+.di-topic-change { font-weight: 700; color: var(--green); }
 
 .text-green { color: var(--green); }
 .text-red { color: var(--red); }
 
-/* Market Opportunity Cards */
+/* -- Opportunity Cards -- */
 .di-opportunity-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 14px; }
-.di-opportunity-card { padding: 16px; border-radius: var(--radius-md); border-left: 4px solid; }
-.di-opportunity-card h5 { font-size: 15px; font-weight: 600; margin: 0 0 6px; }
-.di-opportunity-card p { font-size: 14px; color: var(--text-secondary); line-height: 1.5; margin: 0; }
-.di-opp-green { background: var(--green-light); border-color: var(--green); }
-.di-opp-blue { background: var(--blue-light); border-color: var(--blue); }
-.di-opp-orange { background: var(--orange-light); border-color: #E17055; }
-.di-opp-purple { background: var(--primary-bg); border-color: var(--primary); }
-
-/* Strategic Recommendation */
+.di-opportunity-card {
+  padding: 18px;
+  border-radius: var(--radius-lg);
+  border-left: 4px solid;
+  transition: transform 0.2s ease, box-shadow 0.2s ease;
+}
+.di-opportunity-card:hover {
+  transform: translateY(-2px);
+  box-shadow: 0 6px 20px rgba(0, 0, 0, 0.06);
+}
+.di-opp-icon { font-size: 20px; margin-bottom: 8px; }
+.di-opportunity-card h5 { font-size: 14px; font-weight: 700; margin: 0 0 6px; }
+.di-opportunity-card p { font-size: 13px; color: var(--text-secondary); line-height: 1.55; margin: 0; }
+.di-opp-green { background: linear-gradient(135deg, #e8fff8 0%, #f4fefb 100%); border-color: var(--green); }
+.di-opp-blue { background: linear-gradient(135deg, #e8f4fd 0%, #f5faff 100%); border-color: var(--blue); }
+.di-opp-orange { background: linear-gradient(135deg, #fff8e7 0%, #fffdf5 100%); border-color: #E17055; }
+.di-opp-purple { background: linear-gradient(135deg, #f0edff 0%, #faf9ff 100%); border-color: var(--primary); }
+
+/* -- Strategic Recommendation -- */
 .di-strategic-card {
-  background: linear-gradient(135deg, #2D3436 0%, #636E72 100%);
+  background: linear-gradient(135deg, #2D3436 0%, #4a5568 100%);
   border-radius: var(--radius-lg);
-  padding: 28px;
+  padding: 30px;
   color: var(--white);
+  box-shadow: 0 8px 30px rgba(0, 0, 0, 0.15);
 }
-.di-strategic-card h4 { display: flex; align-items: center; gap: 10px; font-size: 18px; margin: 0 0 14px; color: var(--white); }
+.di-strategic-card h4 { display: flex; align-items: center; gap: 10px; font-size: 18px; margin: 0 0 16px; color: var(--white); }
 .di-strategic-card h5 { font-size: 16px; font-weight: 600; margin: 0 0 10px; color: var(--white); }
-.di-strategic-card p { font-size: 14px; color: rgba(255,255,255,0.8); line-height: 1.6; margin: 0 0 20px; }
+.di-strategic-card p { font-size: 14px; color: rgba(255,255,255,0.82); line-height: 1.7; margin: 0 0 22px; }
 .di-strategic-stats {
   display: grid;
   grid-template-columns: repeat(3, 1fr);
   gap: 14px;
   text-align: center;
-  border-top: 1px solid rgba(255,255,255,0.2);
-  padding-top: 20px;
+  border-top: 1px solid rgba(255,255,255,0.15);
+  padding-top: 22px;
 }
-.di-s-value { font-size: 24px; font-weight: 800; color: var(--white); }
+.di-s-value { font-size: 22px; font-weight: 800; color: var(--white); word-break: break-word; }
 .di-s-value.green { color: var(--green); }
-.di-s-label { font-size: 12px; color: rgba(255,255,255,0.6); margin-top: 4px; }
+.di-s-label { font-size: 12px; color: rgba(255,255,255,0.55); margin-top: 4px; font-weight: 600; }
 
 /* Floating Ask Expert Button */
 .di-ask-expert-float {
@@ -1719,7 +1816,7 @@ code {
   }
 
   .site-header-inner,
-  .app-header-inner {
+  .app-header {
     flex-wrap: wrap;
   }
 
