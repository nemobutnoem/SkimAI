From 903370e16cbe03fad6e43524c8aeecac545f4018 Mon Sep 17 00:00:00 2001
From: nemobutnoem <hoangnvse182984@fpt.edu.vn>
Date: Mon, 23 Mar 2026 20:53:31 +0700
Subject: [PATCH] update

---
 FE/src/styles/global.css | 203 ++++++++++++++++++++++++++-------------
 1 file changed, 138 insertions(+), 65 deletions(-)

diff --git a/FE/src/styles/global.css b/FE/src/styles/global.css
index 9e49dcb..041cb3f 100644
--- a/FE/src/styles/global.css
+++ b/FE/src/styles/global.css
@@ -761,16 +761,34 @@ textarea { resize: vertical; }
 }
 
 .analysis-keyword-shelf {
-  margin-top: 18px;
-  padding-top: 16px;
+  margin-top: 22px;
+  padding-top: 20px;
   border-top: 1px solid var(--border-color);
 }
 
+.kw-shelf-header {
+  display: flex;
+  align-items: flex-start;
+  justify-content: space-between;
+  gap: 12px;
+  margin-bottom: 16px;
+}
+
 .analysis-subsection-title {
-  font-size: 13px;
+  font-size: 15px;
   font-weight: 700;
-  color: var(--text-secondary);
-  margin-bottom: 10px;
+  color: var(--text-primary);
+}
+
+.kw-count-badge {
+  display: inline-flex;
+  padding: 4px 12px;
+  border-radius: var(--radius-full);
+  background: var(--primary-bg);
+  color: var(--primary-dark);
+  font-size: 12px;
+  font-weight: 700;
+  white-space: nowrap;
 }
 
 .analysis-market-signals,
@@ -829,46 +847,66 @@ textarea { resize: vertical; }
 }
 
 .ai-summary {
-  padding: 20px;
+  padding: 24px;
 }
 
+/* ── Insight Cards ── */
 .insight-grid {
   display: grid;
   grid-template-columns: repeat(2, minmax(0, 1fr));
-  gap: 12px;
+  gap: 14px;
 }
 
 .insight-item {
-  padding: 14px;
-  border-radius: var(--radius-md);
+  padding: 18px 18px 16px;
+  border-radius: var(--radius-lg);
   border: 1px solid var(--border-color);
+  border-left: 4px solid var(--primary);
+  transition: transform 0.2s ease, box-shadow 0.2s ease;
+  position: relative;
+  overflow: hidden;
+}
+
+.insight-item:hover {
+  transform: translateY(-2px);
+  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.06);
 }
 
-.insight-item:nth-child(1) { background: var(--primary-bg); }
-.insight-item:nth-child(2) { background: var(--blue-light); }
-.insight-item:nth-child(3) { background: var(--green-light); }
-.insight-item:nth-child(4) { background: var(--orange-light); }
+.insight-item--0 { background: linear-gradient(135deg, #f0edff 0%, #faf9ff 100%); border-left-color: var(--primary); }
+.insight-item--1 { background: linear-gradient(135deg, #e8f4fd 0%, #f5faff 100%); border-left-color: var(--blue); }
+.insight-item--2 { background: linear-gradient(135deg, #e8fff8 0%, #f4fefb 100%); border-left-color: var(--green); }
+.insight-item--3 { background: linear-gradient(135deg, #fff8e7 0%, #fffdf5 100%); border-left-color: #e17055; }
 
-.insight-label {
+.insight-header {
   display: flex;
   align-items: center;
-  gap: 8px;
-  font-size: 12px;
-  font-weight: 700;
-  margin-bottom: 8px;
+  gap: 10px;
+  margin-bottom: 10px;
 }
 
-.dot-indicator {
-  width: 8px;
-  height: 8px;
-  border-radius: 50%;
-  background: var(--primary);
+.insight-icon {
+  width: 32px;
+  height: 32px;
+  border-radius: 10px;
+  background: rgba(255, 255, 255, 0.7);
+  display: inline-flex;
+  align-items: center;
+  justify-content: center;
+  font-size: 16px;
+  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.06);
 }
 
-.insight-text {
-  font-size: 15px;
+.insight-label {
+  font-size: 13px;
+  font-weight: 700;
   color: var(--text-primary);
-  line-height: 1.45;
+  letter-spacing: 0.01em;
+}
+
+.insight-text {
+  font-size: 14px;
+  color: var(--text-secondary);
+  line-height: 1.55;
 }
 
 .see-more-btn {
@@ -878,7 +916,7 @@ textarea { resize: vertical; }
   font-size: 12px;
   font-weight: 600;
   cursor: pointer;
-  padding: 4px 0 0;
+  padding: 6px 0 0;
   display: inline-block;
 }
 
@@ -886,17 +924,18 @@ textarea { resize: vertical; }
   text-decoration: underline;
 }
 
+/* ── Ask AI CTA ── */
 .ask-more {
   display: flex;
   justify-content: flex-end;
-  margin-top: 10px;
+  margin-top: 14px;
 }
 
 .ask-more-cta {
   display: inline-flex;
   align-items: center;
   gap: 10px;
-  padding: 10px 14px;
+  padding: 12px 20px;
   border-radius: 999px;
   color: var(--white);
   font-size: 13px;
@@ -908,20 +947,13 @@ textarea { resize: vertical; }
 
 .ask-more-cta:hover {
   color: var(--white);
-  transform: translateY(-1px);
-  box-shadow: 0 12px 24px rgba(108, 92, 231, 0.35);
+  transform: translateY(-2px);
+  box-shadow: 0 12px 28px rgba(108, 92, 231, 0.4);
 }
 
 .ask-more-icon {
-  width: 22px;
-  height: 22px;
-  border-radius: 7px;
-  background: rgba(255, 255, 255, 0.22);
-  display: inline-flex;
-  align-items: center;
-  justify-content: center;
-  font-size: 10px;
-  letter-spacing: 0.2px;
+  font-size: 16px;
+  line-height: 1;
 }
 
 .trend-area {
@@ -1263,14 +1295,58 @@ textarea { resize: vertical; }
   font-size: 12px;
 }
 
-.keyword-list {
+/* ── Keyword Table ── */
+.keyword-table {
+  border: 1px solid var(--border-color);
+  border-radius: var(--radius-lg);
+  overflow: hidden;
+}
+
+.kw-table-head {
   display: grid;
-  gap: 12px;
+  grid-template-columns: 40px 1.5fr repeat(4, 80px) 1fr;
+  gap: 0;
+  padding: 10px 16px;
+  background: var(--gray-50);
+  border-bottom: 1px solid var(--border-color);
 }
 
-.keyword-row {
+.kw-th {
+  font-size: 11px;
+  font-weight: 700;
+  text-transform: uppercase;
+  letter-spacing: 0.06em;
+  color: var(--text-secondary);
+}
+
+.kw-th-metric, .kw-th-bar { text-align: center; }
+.kw-th-rank { text-align: center; }
+
+.kw-table-row {
   display: grid;
-  gap: 6px;
+  grid-template-columns: 40px 1.5fr repeat(4, 80px) 1fr;
+  gap: 0;
+  padding: 12px 16px;
+  border-bottom: 1px solid var(--border-color);
+  align-items: center;
+  transition: background 0.15s ease;
+}
+
+.kw-table-row:last-child { border-bottom: none; }
+.kw-table-row:hover { background: var(--gray-50); }
+
+.kw-rank {
+  width: 24px;
+  height: 24px;
+  border-radius: 8px;
+  background: var(--primary-bg);
+  color: var(--primary-dark);
+  font-size: 12px;
+  font-weight: 700;
+  display: inline-flex;
+  align-items: center;
+  justify-content: center;
+  margin: 0 auto;
 }
 
 .kw-name {
@@ -1279,34 +1355,31 @@ textarea { resize: vertical; }
   font-size: 14px;
 }
 
-.kw-bar-wrap {
-  display: flex;
-  flex-direction: column;
-  gap: 4px;
+.kw-metric-val {
+  text-align: center;
+  font-size: 13px;
+  color: var(--text-secondary);
+  font-weight: 500;
+  font-variant-numeric: tabular-nums;
 }
 
-.kw-bar {
-  height: 6px;
-  border-radius: var(--radius-full);
-  background: var(--primary-gradient);
-  min-width: 20px;
-  transition: width 0.3s ease;
+.kw-metric-mentions {
+  color: var(--primary-dark);
+  font-weight: 700;
 }
 
-.kw-metrics {
+.kw-bar-cell {
   display: flex;
-  flex-wrap: wrap;
-  gap: 10px;
-  font-size: 11px;
-  color: var(--text-secondary);
-  font-weight: 500;
+  align-items: center;
+  padding: 0 8px;
 }
 
-.kw-metrics span {
-  display: inline-flex;
-  align-items: center;
-  gap: 3px;
-  cursor: default;
+.kw-bar {
+  height: 8px;
+  border-radius: var(--radius-full);
+  background: var(--primary-gradient);
+  min-width: 12px;
+  transition: width 0.4s ease;
 }
 
 .sentiment-container {
-- 
2.51.0.windows.1

